/**
 * Master Camiguin tourism map vector layers (1024×721).
 * Traced from the reference artwork for geometry only — the runtime map is pure SVG.
 */
export {
  MAP_NATIVE,
  TRACED_MAIN_ISLAND,
  TRACED_WHITE_ISLAND,
  TRACED_MANTIGUE,
  TRACED_ROADS,
  TRACED_MUNICIPALITIES,
  TRACED_ATTRACTION_POINTS,
} from "./traced-paths";
